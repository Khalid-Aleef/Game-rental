   
        <!-- Display error message if any -->
        <!-- <?php if (isset($_GET['error']) && $_GET['error'] == 'invalid'): ?> -->
            <!-- <p style="color: red;">Username or password is incorrect. Please try again.</p> -->
        <!-- <?php endif; ?> -->






        home
        <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Silkscreen:wght@400;700&display=swap" rel="stylesheet">
    <title>GameNest Home</title>
    <link rel="stylesheet" href="style.css">
    <style>
        body {
            background-image: url('bg_img.jpg');
            background-size: cover;
            background-repeat: no-repeat;
            background-position: center;
            display: flex;
            flex-direction: column;
            align-items: center;
            height: 100vh;
            margin: 0;
            color: white;
            padding-top: 100px; /* Added padding to the top for more space */
        }
        h1 {
            font-family: 'Silkscreen', sans-serif;
            color: white;
            font-size: 70px;
            margin: 0;
            text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.5);
            position: absolute;
            top: 30px; /* Adjusted the top position to create space */
            left: 20px;
        }
        nav {
            position: absolute;
            top: 120px;
            left: 20px;
        }
        nav button {
            background-color: rgb(28, 159, 67, 0.9);
            color: white;
            border: none;
            padding: 10px 15px;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
            font-family: 'Silkscreen', sans-serif;
            margin: 0 5px;
            opacity: 1; /* Default opacity */
        }
        nav button:hover {
            background-color: rgb(5, 101, 7);
        }
        nav button.active {
            background-color: rgb(5, 101, 7); /* Slightly darker green for active */
            opacity: 0.7; /* Dimmed appearance */
            cursor: default; /* No pointer for active button */
            pointer-events: none; /* Prevent clicks on the active button */
        }
        .search-container {
            position: absolute;
            top: 20px;
            right: 20px;
        }
        .search-container input {
            padding: 10px;
            width: 200px;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 16px;
            font-family: 'Silkscreen', sans-serif;
        }
        .search-container button {
            background-color: rgb(28, 159, 67, 0.9);
            color: white;
            border: none;
            padding: 10px 15px;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
            font-family: 'Silkscreen', sans-serif;
        }
        .search-container button:hover {
            background-color: rgb(5, 101, 7);
        }
        .games-container {
            background: rgba(255, 255, 255, 0.3);
            backdrop-filter: blur(10px);
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            width: 90%;
            height: 60%;
            overflow-y: auto;
            margin-top: 50px;
        }
        .game-list {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
        }
        .game {
            background: rgba(0, 0, 0, 0.6);
            padding: 15px;
            border-radius: 8px;
            text-align: center;
        }
        .game img {
            width: 100%;
            height: auto;  /* Maintain aspect ratio */
            max-height: 150px; /* Limit the height to 150px, adjust as needed */
            object-fit: contain; /* Ensure the entire image is visible */
            border-radius: 8px;
        }
        h2 {
            font-family: 'Silkscreen', sans-serif;
            
        }
        h4 {
            font-family: 'Silkscreen', sans-serif;
            font-size: small;
            
        }
        
        .game h3 {
            font-family: 'Silkscreen', sans-serif;
            margin: 10px 0;
        }
        .game button {
            background-color: rgb(28, 159, 67, 0.9);
            color: white;
            border: none;
            padding: 10px 15px;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
            font-family: 'Silkscreen', sans-serif;
        }
        .game button:hover {
            background-color: rgb(5, 101, 7);
        }
    </style>
</head>
<body>
    <h1>GameNest</h1>
    <nav>
        <button onclick="navigateTo('library')">Library</button>
        <button onclick="navigateTo('account')">Account</button>
        <button onclick="navigateTo('settings')">Settings</button>
        <button id="gamesButton" class="active" onclick="navigateTo('games')">Games</button>
    </nav>
    <div class="search-container">
        <input type="text" placeholder="Search for a game...">
        <button>Search</button>
    </div>


    
    <div id="availableGames" class="games-container">
        <h2>Available Games</h2>
        <div class="game-list">
            <div class="game">
                <img src="game1.jpg.jpg" alt="Game 1">
                <h3>Marvel's Spider Man</h3>
                <h4>Price: 50 BDT/Day</h4>
                <a href="info.php" class="signup-button">Rent Now</a>
            </div>
            <div class="game">
                <img src="game2.jpeg" alt="Game 2">
                <h3>Assassin's Creed Mirage</h3>
                <h4>Price: 40 BDT/Day</h4>
                <a href="modal.php" class="signup-button">Rent Now</a>
            </div>
            <div class="game">
                <img src="game3.jpeg" alt="Game 3">
                <h3>Mortal Kombat 1</h3>
                <h4>Price: 60 BDT/Day</h4>
                <a href="modal.php" class="signup-button">Rent Now</a>
            </div>
            <!-- Add more games as needed -->
        </div>
    </div>
    <script>
        function navigateTo(section) {
            alert(`Navigating to ${section}...`);
        }
    </script>
</body>
</html>








<style>
        body {
            background-image: url('bg_img.jpg');
            background-size: cover;
            background-repeat: no-repeat;
            background-attachment: fixed;
            background-position: center;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            color: white;
            font-family: 'Silkscreen', sans-serif;
        }
        h1 {
            margin-top: 20px;
            text-align: center;
            font-size: 36px;
        }
        .game-details {
            background: rgba(0, 0, 0, 0.8);
            padding: 15px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(255, 255, 255, 0.3);
            width: 90%;
            max-width: 400px;
            text-align: center;
        }
        h2, h3, label {
            margin-bottom: 10px;
        }
        input[type="date"], input[type="text"], button {
            width: 97%;
            padding: 8px;
            margin: 8px 0;
            border-radius: 5px;
            border: 1px solid #ccc;
            font-size: 14px;
        }
        button {
            background-color: rgb(28, 159, 67);
            color: white;
            border: none;
            cursor: pointer;
            font-size: 16px;
            padding: 10px;
        }
        button:hover {
            background-color: rgb(5, 101, 7);
        }
        .total-price {
            margin-top: 10px;
            font-size: 16px;
            font-weight: bold;
        }
    </style>